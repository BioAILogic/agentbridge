version: "2"
sql:
  - engine: "postgresql"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "."
        emit_prepared_queries: false
        emit_interface: false
        emit_exact_table_names: false
